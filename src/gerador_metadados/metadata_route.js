"use strict";

/*
  const nunjucks = require('nunjucks');
  app.set('templates', path.join(__dirname, 'src', 'templates'));  
  nunjucks.configure(path.join(__dirname, 'src', 'templates'), {
    autoescape: true,
    express: app
  });
  
  
  app.use(express.static(path.join(__dirname, 'public')))

router.get('/metadados/:projeto/:mi/:tipo', function(req, res, next) {
    if (['ci_25k', 'ram_50k', 'sc_25k'].indexOf(req.params.projeto) !== -1 &&
        ['matricial', 'vetorial'].indexOf(req.params.tipo) !== -1 ) {
      operations.generateMetadata(req,res,next, req.params.projeto, req.params.mi, req.params.tipo);  
      
    } else {
      next()
    }
});
*/

