# Configuração APENAS para a suíte de testes (valores dummy, NÃO são segredos reais).
# Carregado por src/config.js quando NODE_ENV=test. O banco é mockado nos testes,
# então estes valores não precisam apontar para nada real.
PORT=3013
DB_SERVER=localhost
DB_PORT=5432
DB_NAME=sap_test
DB_NAME_MICROCONTROLE=sap_microcontrole_test
DB_USER=sap_test_user
DB_PASSWORD=dummy_test_password
JWT_SECRET=test_jwt_secret_do_not_use_in_prod
AUTH_SERVER=http://localhost:9999/auth
